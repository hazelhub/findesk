# (선택) 개발자 도구 wrangler 로 배포할 때 쓰는 설정입니다. 대시보드에 코드를 붙여넣는다면 필요 없습니다.
name = "findesk-editor"
main = "worker.js"
compatibility_date = "2026-09-01"

[vars]
ALLOWED_ORIGIN = ""
# 비밀값: npx wrangler secret put EDITOR_TOKEN
